import { render, fireEvent, screen } from "@testing-library/react";

jest.mock("../libs/axios");

describe("Component", () => {
  describe("when", () => {
    test("then", () => {
      // render component
      
      // select

      // fire event

      // assertion
    });
  });

  describe("when error", () => {
    test("then", () => {
      // render component

      // select
      
      // fire event

      // error essertion
    });
  });
});
