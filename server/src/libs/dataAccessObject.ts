class DAO {
  // data props
  constructor() {
    // local data storage
  }

  // CRUD data functions

  reset() {
    // reset any stored data
  }
}

const DataAccessObject = new DAO();
export default DataAccessObject;
